# مكتب السواعد للمحاماة والاستشارات القانونية والتحكيم

موقع ويب احترافي متكامل لمكتب محاماة، مبني باللغة العربية (RTL) بتصميم حديث وفخم.

## 🛠️ التقنيات المستخدمة

- **Next.js 15** (App Router)
- **TypeScript**
- **Tailwind CSS**
- **Framer Motion** (للحركات والانتقالات)
- **React Icons**
- خطوط عربية احترافية: **Cairo** و **Tajawal** (عبر `next/font`)

## 🎨 الهوية البصرية

| اللون | الكود |
| ----- | ----- |
| كحلي داكن | `#0F172A` |
| ذهبي | `#C9A227` |
| أبيض | `#FFFFFF` |

## 🚀 التشغيل

```bash
# 1. تثبيت الحزم
npm install

# 2. تشغيل بيئة التطوير
npm run dev
# الموقع على: http://localhost:3000

# 3. بناء نسخة الإنتاج
npm run build
npm start
```

## 📁 هيكل المشروع

```
app/
├── layout.tsx            # التخطيط الرئيسي + الخطوط + SEO
├── page.tsx              # الصفحة الرئيسية
├── globals.css           # الأنماط العامة
├── about/page.tsx        # صفحة من نحن
├── services/page.tsx     # صفحة الخدمات
├── team/page.tsx         # صفحة فريق العمل
├── contact/page.tsx      # صفحة تواصل معنا
├── blog/
│   ├── page.tsx          # قائمة المقالات (بحث + تصنيفات)
│   └── [slug]/page.tsx   # صفحة المقال المنفصلة
├── sitemap.ts / robots.ts # ملفات SEO
└── not-found.tsx         # صفحة 404

components/
├── layout/   (Header, Footer)
├── sections/ (أقسام الصفحات)
├── ui/       (مكونات واجهة قابلة لإعادة الاستخدام)
└── forms/    (ContactForm مع التحقق من البيانات)

lib/
├── data.ts   # جميع البيانات التجريبية (Mock Data)
├── types.ts  # تعريفات TypeScript
├── motion.ts # إعدادات الحركات
└── utils.ts  # دوال مساعدة
```

## ✏️ التخصيص

### تعديل بيانات المكتب
كل المحتوى في ملف واحد: **`lib/data.ts`**
- `siteConfig` — الاسم، الهاتف، البريد، العنوان، وسائل التواصل
- `services` — الخدمات القانونية
- `team` — أعضاء الفريق
- `testimonials` — آراء العملاء
- `faqs` — الأسئلة الشائعة
- `articles` — المقالات

### استبدال صور الفريق
الصور حاليًا عبارة عن **Placeholder** قابلة للاستبدال بسهولة دون أي تعديل على التصميم.
في `lib/data.ts`، غيّر قيمة `image` لكل عضو إلى مسار صورتك:

```ts
{
  id: 1,
  name: "أ. عبدالله السواعد",
  image: "/team/abdullah.jpg", // ضع الصورة في مجلد public/team
  ...
}
```

### خريطة Google Maps
في `components/sections/MapPlaceholder.tsx` استبدل رابط `iframe` بكود التضمين
الخاص بموقعك من خرائط Google.

### نموذج التواصل
نموذج التواصل يحتوي على تحقق كامل من البيانات. الإرسال حاليًا تجريبي (Mock).
اربط الإرسال بواجهة API الحقيقية في `components/forms/ContactForm.tsx`
داخل دالة `handleSubmit`.

## ✅ المميزات

- تصميم متجاوب بالكامل (جوال، تابلت، كمبيوتر)
- دعم RTL كامل
- حركات وانتقالات ناعمة (Framer Motion)
- تأثيرات Hover احترافية
- عدادات متحركة للإحصائيات
- سلايدر آراء العملاء
- أكورديون للأسئلة الشائعة
- نظام مدونة مع بحث وتصنيفات
- نموذج تواصل مع تحقق من البيانات
- جاهز لمحركات البحث (SEO + Sitemap + Robots)
- كود منظم وقابل للتطوير

---

© جميع الحقوق محفوظة — مكتب السواعد للمحاماة.
